# Hướng dẫn Đóng góp (CONTRIBUTING)

Chào mừng bạn đến với **business-skills**! Đây là một thư viện modular business skills được thiết kế để giúp các chủ doanh nghiệp và marketer vận hành AI hiệu quả hơn.

## Cách thêm một Skill mới

Để thêm một skill mới vào thư viện, vui lòng tuân thủ cấu trúc sau:

1. **Vị trí**: Lưu file vào thư mục `skills/[tên-skill].md`.
2. **Cấu trúc File**:
   - `<identity>`: Định nghĩa vai trò của AI.
   - `<context>`: Bối cảnh sử dụng.
   - `<mission>`: Nhiệm vụ cụ thể.
   - `<rules>`: Các quy tắc và ràng buộc.
   - `<output_format>`: Định dạng đầu ra mong muốn.
   - `<related_skills>`: Các skill liên quan trong repo.
3. **Ngôn ngữ**: Sử dụng tiếng Việt chuyên nghiệp, thực tế và dễ hiểu.

## Quy trình đóng góp

1. Fork repository này.
2. Tạo một branch mới: `git checkout -b feature/them-skill-moi`.
3. Thực hiện thay đổi và commit: `git commit -m "Thêm skill: [Tên Skill]"`.
4. Push lên branch: `git push origin feature/them-skill-moi`.
5. Tạo một Pull Request.

Cảm ơn bạn đã đóng góp!
